import {BookText, FilePen, GraduationCap, Phone, Users, type LucideIcon} from "lucide-react";

export const admissionIconMap: Record<string, LucideIcon> = {
  BookText,
  FilePen,
  GraduationCap,
  Phone,
  Users,
};
