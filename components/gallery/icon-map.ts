import {BookOpen, GraduationCap, HeartPulse, LibraryBig, School, Stethoscope, type LucideIcon} from "lucide-react";

export const galleryIconMap: Record<string, LucideIcon> = {
  BookOpen,
  GraduationCap,
  HeartPulse,
  LibraryBig,
  School,
  Stethoscope,
};
