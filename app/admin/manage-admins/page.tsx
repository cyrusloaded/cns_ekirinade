import AdminUsersManager from "@/components/admin/AdminUsersManager";

export const metadata = { title: "Manage Admins — Admin Console" };

export default function ManageAdminsPage() {
  return <AdminUsersManager />;
}
